// reporting_app client config
// Reference this file in JS modules with, e.g, `import { api_url } from '../client.config.js';`. Importing from this
// config is enough for it to be picked up by ParcelJS.

export const api_url = 'http://localhost:4999/api/0.1/';
export const eve_date_fmt = 'DD_MM_YYYY_HH:mm:ss';
